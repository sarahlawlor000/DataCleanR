% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardise_missing.R
\name{standardise_missing}
\alias{standardise_missing}
\title{Standardise missing-value encodings to NA}
\usage{
standardise_missing(
  data,
  tokens = c("NA", "N/A", "na", "n/a", "null", "NULL", "none", "None", "not available",
    "Not Available", "missing", "", " "),
  ignore_case = TRUE,
  trim_ws = TRUE,
  convert_nan = TRUE
)
}
\arguments{
\item{data}{A data.frame (or tibble).}

\item{tokens}{Character vector of missing-like strings to convert to \code{NA}.
Matching is applied to character and factor columns.}

\item{ignore_case}{Logical; if TRUE, matching ignores case.}

\item{trim_ws}{Logical; if TRUE, trims leading/trailing whitespace before matching.}

\item{convert_nan}{Logical; if TRUE, converts \code{NaN} in numeric columns to \code{NA}.}
}
\value{
A data.frame of the same class as \code{data} (where possible), with
  missing-like values converted to \code{NA}.
}
\description{
Many datasets use different strings to represent missingness (e.g. "N/A",
"not available", "", "null"). This function converts common missing-like
strings to proper \code{NA} while preserving column types where possible.
Numeric \code{NaN} values are also converted to \code{NA}.
}
\examples{
df <- data.frame(
  a = c("ok", "N/A", "  not available  ", ""),
  b = c(1, NaN, 3, 4),
  c = factor(c("yes", "NA", "no", "null")),
  stringsAsFactors = FALSE
)

cleaned <- standardise_missing(df)
cleaned$a
cleaned$b
cleaned$c

}
